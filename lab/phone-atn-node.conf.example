# Example phone → single hub conf (DEC-0038)
# Copy to lab/phone-atn-node.conf and fill peer_* from: atnnode listen
# peer_ipv4 MUST be the hub PC LAN address (not 127.0.0.1).

peer_ipv4=YOUR_HUB_LAN_IPV4
peer_port=47000
peer_ek=REPLACE_WITH_HEX_FROM_LISTEN
diag=1
flush_mode=log_only
