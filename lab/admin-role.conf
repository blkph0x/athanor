# DEC-0057 — primary admin hub role.
# Only one hub should be role=primary. That hub authors org-policy.conf.
# Secondary hubs adopt higher policy_ver over tunnel; local Security Save is refused.
role=primary
hub_id=lab-hub-1
